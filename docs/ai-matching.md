# AI product matching (frontend)

Canonical reference: [zumo-backend/docs/ai-matching.md](../../zumo-backend/docs/ai-matching.md).

The dashboard uses `MatchCoverageIndicator` for coverage % and the touchless (⚡) badge. WhatsApp sidebar drafts reflect backend **`draftableLines`** — partial matches appear while the bot is still clarifying.
